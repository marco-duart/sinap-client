import { createTheme } from "../stitches.config";

export const darkTheme = createTheme({
  colors: {
    primary: "#00BFA6",
    secondary: "#011F2C",
    background: "#0A0A0A",
    text: "#EAEAEA",
  },
});
